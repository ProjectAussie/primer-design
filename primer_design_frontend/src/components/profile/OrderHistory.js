import { Typography } from "@mui/material"

const OrderHistory = () => {
  return (
  <>
    <Typography variant="h5" color="primary">
      Your Order History:
    </Typography> 
    <Typography variant="body1">
      Not currently tracking order history. To be implemented.
    </Typography>
  </> 
  )
}

export default OrderHistory